{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug C++",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}", // Lấy đường dẫn file thực thi từ CMake Tools
            "args": [],
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
			"internalConsoleOptions": "neverOpen"
        }
    ]
}